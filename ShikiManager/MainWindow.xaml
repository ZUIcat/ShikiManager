<Window x:Class="ShikiManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShikiManager"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="450"
        Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Button Click="TestButton01_Click" Grid.Column="0" Grid.Row="0">TestButton01</Button>
        <Button Click="TestButton02_Click" Grid.Column="0" Grid.Row="1">TestButton02</Button>
        <Button Click="TestButton03_Click" Grid.Column="0" Grid.Row="2">TestButton03</Button>

        <Button Click="TestButton04_Click" Grid.Column="1" Grid.Row="0">TestButton04</Button>
        <Button Click="TestButton05_Click" Grid.Column="1" Grid.Row="1">TestButton05</Button>
        <Button Click="TestButton06_Click" Grid.Column="1" Grid.Row="2">TestButton06</Button>
    </Grid>
</Window>
